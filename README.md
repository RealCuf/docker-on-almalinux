# 🐳 Docker چیست؟

داکر یک پلتفرم متن‌باز است که امکان **ساخت، اجرا و مدیریت برنامه‌ها در قالب کانتینر** را فراهم می‌کند.  
کانتینرها محیط‌هایی سبک، سریع و ایزوله هستند که برنامه را همراه با تمام وابستگی‌هایش اجرا می‌کنند.

---

## 📦 کانتینر چیست؟

کانتینر مثل یک "جعبه کوچک" است که شامل:

- نسخه مناسب از زبان برنامه‌نویسی
- کتابخانه‌ها و پکیج‌ها
- کانفیگ‌ها
- کد برنامه

می‌شود.  
برنامه داخل کانتینر **در هر سیستم‌عاملی دقیقاً یکسان اجرا می‌شود**.

🔹 یعنی دیگر خبری از این جمله‌ها نیست:

> "روی سیستم من کار می‌کنه ولی روی سرور نه!"  
> "نسخه‌ی پایتون فرق داشت!"  
> "کتابخانه نصب نبود!"

---

## ⚡ تفاوت Docker با ماشین مجازی (VM)

| ویژگی | Docker (کانتینر) | ماشین مجازی (VM) |
|------|------------------|------------------|
| سرعت اجرا | بسیار سریع | معمولی تا کند |
| مصرف منابع | کم | زیاد |
| حجم سیستم | ده‌ها مگابایت | چند گیگ |
| نیاز به سیستم‌عامل | فقط کرنل میزبان | سیستم‌عامل کامل جداگانه |
| زمان بوت | چند ثانیه | ۳۰–۶۰ ثانیه |

Docker سبک‌تر، سریع‌تر و برای توسعه و دیپلوی مدرن مناسب‌تر است.

---

## 🎯 Docker چه کاربردهایی دارد؟

- اجرای برنامه‌ها بدون نصب وابستگی‌ها
- اجرای سرویس‌هایی مثل Redis، MongoDB، MySQL با یک دستور
- راحت‌ترین روش برای اجرای پروژه‌ها روی سرور
- مناسب برای DevOps، CI/CD و میکروسرویس‌ها
- اجرای ربات‌های تلگرام، سایت‌ها، APIها و اسکریپت‌ها
- جلوگیری از بهم‌ریختگی سیستم اصلی

---


# راهنمای نصب Docker روی AlmaLinux 10

این آموزش شامل مراحل کامل و ساده برای نصب Docker روی AlmaLinux 10 و اجرای یک کانتینر است.

---

## 📌 پیش‌نیازها

* سیستم AlmaLinux 10
* دسترسی کاربر با مجوز **sudo**
* اتصال به اینترنت

---

## 1️⃣ مرحله : به‌روزرسانی مخازن بسته‌ها

ابتدا بسته‌های سیستم رو بروز کن تا مطمئن بشی همه چیز به‌روز است.

```bash
sudo dnf update
```

![Preview](https://cdn.shortpixel.ai/spai/q_lossy+ret_img+to_auto/linuxiac.com/wp-content/uploads/2025/06/alma-docker-install-01-1024x211.jpg)

---

## 2️⃣ مرحله 2: اضافه کردن مخزن رسمی Docker

ابتدا مخزن رسمی Docker را اضافه می‌کنیم:

```bash
sudo dnf config-manager --add-repo https://download.docker.com/linux/centos/docker-ce.repo
```

> توجه: AlmaLinux با CentOS سازگاری دارد و از همان مخزن استفاده می‌کند.

![Preview](https://linuxiac.com/wp-content/uploads/2025/06/alma-docker-install-02.jpg)
---

## 3️⃣ مرحله 3: دوباره بروز کردن لیست بسته‌ها
پس از افزودن مخزن، دوباره لیست بسته‌ها/مخازن را بروز کن تا سیستم بتواند بسته‌های Docker را ببیند:

```bash
sudo dnf update
```

![Preview](https://cdn.shortpixel.ai/spai/q_lossy+ret_img+to_auto/linuxiac.com/wp-content/uploads/2025/06/alma-docker-install-03.jpg)

---
## 4️⃣ مرحله 4: نصب Docker و ابزارهای جانبی

حالا Docker و اجزای لازمش را نصب کن:
```bash
sudo dnf install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

![Preview](https://cdn.shortpixel.ai/spai/q_lossy+ret_img+to_auto/linuxiac.com/wp-content/uploads/2025/07/alma-docker-install-05-1024x583.jpg)
![Preview](https://cdn.shortpixel.ai/spai/q_lossy+ret_img+to_auto/linuxiac.com/wp-content/uploads/2025/07/alma-docker-install-07-1024x238.jpg)


## 5️⃣ مرحله 5: فعال‌سازی و شروع سرویس Docker

بعد نصب، باید سرویس Docker را فعال کنی تا همین حالا اجرا بشود و در بوت بعدی هم خودکار شروع شود:
```bash
sudo systemctl enable --now docker
```

![Preview](https://cdn.shortpixel.ai/spai/q_lossy+ret_img+to_auto/linuxiac.com/wp-content/uploads/2025/07/alma-docker-install-08-1024x112.jpg)

برای بررسی وضعیت سرویس هم می‌توانی از این دستور استفاده کنی:
```bash
sudo systemctl status docker
```

![Preview](https://cdn.shortpixel.ai/spai/q_lossy+ret_img+to_auto/linuxiac.com/wp-content/uploads/2025/07/alma-docker-install-09-1024x553.jpg)

---
## 6️⃣ مرحله : تست اینکه Docker درست نصب شده

ساده‌ترین راه: اجرای تصویری به‌نام hello-world — اگر پیام خوش‌آمدگویی دیدی یعنی نصب موفقیت‌آمیز بود:
```bash
sudo docker run hello-world
```

![Preview](https://cdn.shortpixel.ai/spai/q_lossy+ret_img+to_auto/linuxiac.com/wp-content/uploads/2025/07/alma-docker-install-11-1024x738.jpg)

---

## 7️⃣ مرحله 7: اجرای کاربر بدون sudo (اختیاری و توصیه‌شده)

![Preview](https://cdn.shortpixel.ai/spai/q_lossy+ret_img+to_auto/linuxiac.com/wp-content/uploads/2025/07/alma-docker-install-12-1024x140.jpg)

به‌صورت پیش‌فرض فقط کاربر root (یا با sudo) می‌تواند دستورهای Docker را اجرا کند. اگر می‌خوای کاربر معمولی هم بدون sudo از Docker استفاده کند:
```bash
sudo usermod -aG docker $USER
newgrp docker
```

![Preview](https://cdn.shortpixel.ai/spai/q_lossy+ret_img+to_auto/linuxiac.com/wp-content/uploads/2025/07/alma-docker-install-13.jpg)

---

## 🚀 تست Docker: اجرای یک کانتینر نمونه (Ubuntu)

#### برای اجرای یک کانتینر سبک

```bash
docker run -it ubuntu
```

#### برای خروج

```bash
exit
```
#### برای خروج از کانتینر بدون اینکه آن را متوقف کنی

```bash
Ctrl + P + Q
```
---
### دیدن کانتینرهای در حال اجرا

```bash
docker ps
```
#### برای دیدن همه کانتینرها (حتی متوقف‌ها)
```bash
docker ps -a
```
---

#### نام‌گذاری کانتینر
```bash
docker run -it --name mycontainer ubuntu
```
---

#### اجرای دستورات بدون ورود به کانتینر
مثلاً می‌خوای داخل کانتینر دستور ls رو اجرا کنی بدون اینکه وارد بشی:
```bash
docker run ubuntu ls /
```
---

## 📌 اجرای یک کانتینر Nginx


```bash
docker run -d -p 8080:80 --name mynginx nginx
```

## 📝 توضیح گزینه‌های دستور

### **docker run**

اجرای یک کانتینر جدید بر اساس یک ایمیج.

### **-d**

اجرای کانتینر در پس‌زمینه (Detached Mode).

### **-p 8080:80**

پورت‌ها:

* **80**: پورت داخلی کانتینر که Nginx روی آن اجرا می‌شود.
* **8080**: پورت میزبان برای دسترسی از مرورگر.

نتیجه: باز کردن آدرس زیر سرویس Nginx را نمایش می‌دهد:

```
http://localhost:8080 / http://ip:9000
```

### **--name mynginx**

نام‌گذاری کانتینر با نام دلخواه.

### **nginx**

ایمیج رسمی Nginx از Docker Hub.

---

## 📌 تست سرویس Nginx

حالا مرورگر رو باز کن:

```bash
http://localhost:8080 / http://ip:8080
```
---
### مدیریت کانتینر
دیدن کانتینرهای در حال اجرا:
```bash
docker ps
```
#### توقف کانتینر
```bash
docker stop mynginx
```

#### شروع مجدد کانتینر
```bash
docker start mynginx
```
---
## 📌 اجرای کانتینر Portainer

داشبورد گرافیکی مدیریت Docker

```bash
docker run -d -p 9000:9000 --name portainer \
  --restart=always \
  -v /var/run/docker.sock:/var/run/docker.sock \
  -v portainer_data:/data \
  portainer/portainer-ce
```
## 📌 تست سرویس Portainer

حالا مرورگر رو باز کن:

```bash
http://localhost:9000 / http://ip:9000
```
---

## 🎯 نتیجه‌گیری

در این فایل یاد گرفتید:

* Docker را روی AlmaLinux 10 نصب کنید
* سرویس Docker را اجرا و فعال کنید
* یک کانتینر ابونتو اجرا کنید
*  یک کانتینر وب سرور اجرا کنید
* داخل کانتینر پنل مدیریت داکر اجرا کنید

---
